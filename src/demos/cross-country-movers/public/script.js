const form = document.querySelector("form");

form.addEventListener("submit", (e) => {
  e.preventDefault();
  alert(
    "This is a demo project. You have successfully mimicked a quote request!"
  );
});
